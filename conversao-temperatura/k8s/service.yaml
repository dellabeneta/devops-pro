apiVersion: v1
kind: Service
metadata:
  name: convtemp
spec:
  selector:
    app: convtemp
  type: NodePort
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
    nodePort: 30000
